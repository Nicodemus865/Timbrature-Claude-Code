services:
  - type: web
    name: bustapaga-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    port: 8000
    envVars:
      - key: GEMINI_API_KEY
        sync: false
